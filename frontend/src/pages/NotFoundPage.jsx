const NotFoundPage = () => (
  <div className="text-center">
    <h1 className="h2 text-muted">Страница не найдена</h1>
    <p className="text-muted">
      Перейти на
      {' '}
      <a href="/">главную страницу</a>
    </p>
  </div>
);

export default NotFoundPage;
